let a = 1;
let b = 0 - 3;
let c = 2;
let discriminant: i32 = b * b - 4 * a * c;
let root1 = 0 - b + discriminant.isqrt() / 2 * a;
let root2 = 0 - b - discriminant.isqrt() / 2 * a;
println!("Two distinct real roots:");
println!("{}", root1);
println!("{}", root2);